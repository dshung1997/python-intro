/*
 * @lc app=leetcode id=59 lang=javascript
 *
 * [59] Spiral Matrix II
 */

// @lc code=start
/**
 * @param {number} n
 * @return {number[][]}
 */
var generateMatrix = function(n) {
  const directions = [[0, 1], [1, 0], [0, -1], [-1, 0]] /* (y, x) */
  const bottomLimit = 0;
  const topLimit = n - 1;

  const currentDirectionIndex = 0;

  for(let )
};
// @lc code=end

